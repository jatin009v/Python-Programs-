# Sample lists
keys = ['a', 'b', 'c']
values = [1, 2, 3]

# Create a dictionary by mapping the two lists
result_dict = {}
for i in range(len(keys)):
    result_dict[keys[i]] = values[i]

print(result_dict)
