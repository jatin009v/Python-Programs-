# Define the value you want to fill the list with
specified_value = 42

# Specify the desired length of the list
list_length = 5

# Initialize and fill the list with the specified value using a list comprehension
filled_list = [specified_value] * list_length

# Print the filled list
print("Filled list using list comprehension:", filled_list)
